%% General definitions
%% General definitions
\documentclass{article} %% Determines the general format.
\usepackage{a4wide} %% paper size: A4.
\usepackage{hyperref}
\usepackage[utf8]{inputenc} %% This file is written in UTF-8.
%% Some editors on Windows cannot save files in UTF-8.
%% If there is a problem with special characters not showing up
%% correctly, try switching "utf8" to "latin1" (ISO 8859-1).
\usepackage[T1]{fontenc} %% Format of the resulting PDF file.
\usepackage{fancyhdr} %% Package to create a header on each page.
\usepackage{lastpage} %% Used for "Page X of Y" in the header.
                      %% For this to work, you have to call pdflatex twice.
\usepackage{enumerate} %% Used to change the style of enumerations (see below).

\usepackage{amssymb} %% Definitions for math symbols.
\usepackage{amsmath} %% Definitions for math symbols.

\usepackage{tikz}  %% Pagacke to create graphics (graphs, automata, etc.)
\usetikzlibrary{automata} %% Tikz library to draw automata
\usetikzlibrary{arrows}   %% Tikz library for nicer arrow heads
\usepackage{float}

%% Left side of header
\lhead{\course\\\semester}

\rhead{Page \thepage\ of \pageref{LastPage}}
%% Height of header
\usepackage[headheight=36pt]{geometry}
%% Page style that uses the header
\pagestyle{fancy}

\newcommand{\semester}{Spring Semester 2026}
\newcommand{\course}{DPI project report}
\title{Blabber project report}
\author{Benedikt Karli, Robin Gökcen, Boran Gökcen, Till Küng }
\date{July 19th 2026}

\begin{document}

\maketitle

\section{Introduction}
This is the project report added to our hand in for the Distributed Programming and Internet Architecture project in the spring semester of 2026. Here we will provide technical insight, an overview and background to our implementation of the peer-to-peer chat \textbf{Blabber}.
\subsection{Blabber Background}
We as friends have regularly used Discord and other messenger apps for communication. \\

%% PROPOSAL (typo, still unfixed): "goverment" -> "government".
%% PROPOSAL (typo, still unfixed): "Government Id's" -> "government
%% IDs" (no apostrophe for a plural, and "ID" not "Id").
%% PROPOSAL (unsourced claim): "at least seventy thousand users" is a
%% specific, checkable number with no citation. Either cite a source
%% for it, or soften to something that doesn't need a citation, e.g.
%% "a large number of users' identification documents were leaked".
%% PROPOSAL (citation): "the Iroh protocol, which was introduced during
%% the lecture" only means something to a grader who was in this
%% specific course. Consider citing the Iroh project itself (its
%% website/GitHub/whitepaper) instead of relying on course context, so
%% the report reads as self-contained to an external reader.
With our growing awareness for privacy related threats throughout our studies, we have become more and more concerned about our reliance on companies, which have direct or indirect ties and exposure to goverment and corporate surveillance programs. Because Discord moved to Identity verification using Government Id's and afterwards documents of at least seventy thousand users were leaked, we decided we wanted to move to our own chat application for this project. Because we wanted to rely less on centralized infrastructure, the Iroh protocol, which was introduced during the lecture and with which some of us had already worked before, was decided to be the right fit for our project.\\
In the following chapters we document our implementation journey and actual use-cases for our achieved results.
\section{Problem statement}
Because we planned our chat application with idea of using it ourselves in mind, we had to find out what actually makes a chat comfortable and practical to use. These are what we decided our requirements for the chat application. \\
%% PROPOSAL (typo, still unfixed): item (b) has a duplicated word,
%% "and and" -> "and".
%% PROPOSAL (clarity): items (c) and (d) read as two angles on the same
%% underlying capability (asynchronous / offline-tolerant messaging):
%% (c) is the sender's perspective ("send to someone not connected"),
%% (d) is the receiver's perspective ("receive what you missed"). Iroh
%% Docs' replication model is what actually delivers both, so consider
%% either merging them into one requirement or explicitly noting they
%% describe the same mechanism from two sides, to avoid the reader
%% wondering whether two separate features are required.
Users should be able to:
\begin{enumerate} [a)]
    \item Create group chats for everyone to use with specific chat rooms.
    \item Keep track of messages and and be assured that all participants have the same chat history.
    \item Send messages to users who are not connected through the rooms.
    \item Receive missed messages when we reconnect.
    \item Start a call with another user.
    \item Navigate the user interface while achieving their goals.

\end{enumerate}
 We not only wanted to be able to chat but also create a reasonably safe environment for our chatting history and communication. Therefore we formulated the following privacy and security requirements:
\begin{enumerate} [a)]
    \item Never store user information or related data, except on endpoints of communication.
    \item Prevent information disclosure through data in transit.
    \item Encrypt critical data stored on end devices. Critical data does not include chat history.
    \item Prevent user impersonation by authentication through password, nonce and username.
\end{enumerate}

\section{Iroh as our background technology}
%% PROPOSAL (citation, moved from the old System architecture location
%% now that this section comes first): this is the first mention of
%% Iroh in the report body -- add a citation to the Iroh project here
%% (website/GitHub), e.g. \cite{iroh} once the bibliography from the
%% end of the document is added.
%% PROPOSAL (grammar): "As we will elaborate on further in the
%% following chapters we used the Iroh networking stack..." is missing
%% a comma after "chapters", and "elaborate on further" is redundant
%% ("elaborate" already implies going into more detail). Suggested:
%% "As we will elaborate on in the following chapters, our project is
%% built on the Iroh networking stack."
As we will elaborate on further in the following chapters we used the Iroh networking stack and interfaces in our project.
Iroh provides the following building blocks we composed to achieve our final implementation.
\\ \\
\textbf{Iroh Endpoint} provides the peer discovery and
    establishes the QUIC connections between two users identified by a
    public key: \texttt{EndpointId}, which removes the need for a third party communication handler.
\\ \\
\textbf{Iroh Docs} provides replicated, multi-writer
    key-value documents. That means every document is identified by a name
    and can be shared with others by a \texttt{DocTicket},
    which gives the receiver either read or write access to it.
\\ \\
\textbf{Iroh Gossip} propagates the updates of the documents between
    peers that are subscribed to the same document, without requiring
    everyone to be directly connected to each other.
\\ \\
\textbf{Iroh Blobs} stores the actual content that is referenced by
    document entries. A document entry only stores the hash of the entry, and
    the associated bytes are fetched separately by the blob store.
\\ \\
Blabber's backend combines these four
components inside a single \texttt{Node}, and
adds two custom protocols of its own: one for
peer-to-peer audio transport and a second  one for group-call signaling.


\section{System architecture}
Blabber is implemented as a peer-to-peer desktop application. Instead of relying on a central server, every user runs their own instance of the application and communicates directly with other peers. The application consists of three main parts: the graphical user interface, the backend and the networking layer.\\
The graphical user interface is implemented in Vue. It displays spaces, rooms, messages and voice calls and forwards user actions to the backend through Tauri commands.\\
The backend is implemented in Rust using the Tauri framework. It manages the local application state, encrypted identities and communication with the networking layer.\\
The networking layer is implemented using Iroh, which provides peer discovery, replicated documents and direct communication between peers. Blabber uses Iroh Docs to synchronize shared data, Iroh Gossip for document synchronization, Iroh Blobs for persistent storage and a custom protocol for voice communication.

\subsection{Backend architecture}

As mentioned, the backend is structured around a \textbf{Node} component. The node
coordinates the main application services and provides the connection between
the frontend and the decentralized networking layer. Requests from the Vue
frontend are received through Tauri commands and then forwarded to the
corresponding backend component.
\\
\\
During startup, the node creates an Iroh endpoint from the secret, which is stored in the
user's identity. Since the same secret is reused after every login, the user
receives the same cryptographic endpoint identity. The node then initializes
Iroh Gossip, the persistent blob store, the document engine and the protocol
router. The router accepts the protocols required for document synchronization,
blob transfer, gossip communication and voice calls.
\\
\\
The node keeps track of all
loaded spaces. Each \textbf{Space} is represented by an information document
and a member document. The information document stores room records, while the
member document stores the users who have joined the space. Both documents are
implemented using Iroh Docs and their serialized values are stored through
Iroh Blobs.
\\
\\
Every room is represented by its own replicated Iroh document. When a room is
created, the backend creates a new document and generates a write ticket for it.
A room record is then
stored in the space's information document. It contains the room identifier,name and document ticket. Other peers can read this record,
import the room document and add the room to their local application state.
\\
\\
Messages are serialized using Postcard and written into the document belonging
to the selected room. The document entry contains a reference to the serialized
message data stored in Iroh Blobs. When a room is loaded, the backend queries
the existing entries, retrieves their contents from the blob store and creates
a local in-memory cache of the message history.
\\
\\
Rooms subscribe to live document events provided by Iroh Docs. When a local or
remote message entry is inserted, the backend retrieves and deserializes the
message, updates the room cache and creates an application event. These events
are sent through a Tokio broadcast channel and can be forwarded to the
frontend, allowing the visible chat to update without continuous polling.
\\
\\
Spaces are shared through serialized invites. An invite contains the identifier
and name of the space together with a read ticket for the information document
and a write ticket for the member document. After importing these documents, a
peer can discover the existing rooms from the information document and import
their individual message documents using the stored room tickets.
\subsection{Frontend architecture}

%% PROPOSAL (outdated content): this whole subsection still describes
%% the OLD frontend (blabber-gui), which was replaced by "blabber-app"
%% in the feature/new-gui merge. blabber-gui is no longer even a
%% workspace member. Concretely:
%%  - "divided into different views, including the login page, the
%%    main chat view and the settings page" -- current blabber-app has
%%    only two top-level views (LoginView.vue, MainView.vue); settings
%%    is a drawer (IdentitySettingsDrawer.vue) opened from within the
%%    main view, not a separate page.
%%  - "The root component (App.vue) controls which view is currently
%%    displayed. It switches between the login, chat and settings
%%    views..." -- current App.vue is just `<router-view />`; routing
%%    is now handled by vue-router, not manual view-switching / shared
%%    reactive state in App.vue.
%% Suggested replacement text:
%%
%% The frontend is implemented using Vue 3 and TypeScript. It is
%% responsible for displaying the user interface and handling user
%% interactions. Navigation between the login screen and the main
%% application is handled by vue-router; the root component
%% (\texttt{App.vue}) only renders the router outlet. Once logged in,
%% users interact with a single main view composed of a space sidebar,
%% a room sidebar and the active room's message list, with settings,
%% and space/room creation implemented as dialogs and a drawer layered
%% on top of that view rather than as separate pages.
%% \\
%% \\
%% The frontend is connected to the Rust backend through the Tauri
%% framework. Instead of communicating directly with the networking
%% layer, the frontend calls Tauri commands to perform operations such
%% as creating spaces, joining spaces and sending messages. Backend
%% events (e.g. newly synchronized messages, new members, new rooms)
%% are pushed back to the frontend through a small event bridge that
%% re-emits Blabber's internal application events as a single Tauri
%% event ("app-event"). A Pinia store subscribes to this event once on
%% startup and dispatches each payload variant (NewMessage, NewMember,
%% NewRoom) into the corresponding piece of reactive state (e.g.
%% appending to \texttt{messagesByRoom}), which Vue's reactivity system
%% then propagates to the visible chat automatically. This separates
%% the user interface from the application logic and keeps the
%% networking implementation inside the backend.
%% (This last part gives the Frontend architecture subsection the same
%% level of concrete, code-tied detail as "Backend architecture" above
%% it -- currently the frontend side is comparatively shallow once you
%% strip out the outdated view-switching description.)
%%
%% PROPOSAL (accuracy caveat, ties to PART 0 of the notes file): as of
%% the current frontend, voice-call and call-room Tauri commands are
%% NOT wired up (the corresponding command file exists but is entirely
%% commented out, and there is no call-room command file at all) --
%% so "starting voice calls" should be removed from this paragraph
%% unless those commands are re-added before submission. See the
%% caveat repeated at "Supported chat operations" and "Achieved
%% results" below.
The frontend is implemented using Vue 3 and TypeScript. It is responsible for
displaying the user interface and handling user interactions. The application
is divided into different views, including the login page, the main chat view
and the settings page.
\\
\\
The frontend is connected to the Rust backend through the Tauri framework.
Instead of communicating directly with the networking layer, the frontend calls
Tauri commands to perform operations such as creating spaces, joining spaces,
sending messages and starting voice calls. This separates the user interface
from the application logic and keeps the networking implementation inside the
backend.
\\
\\
The root component (\texttt{App.vue}) controls which view is currently displayed.
It switches between the login, chat and settings views and stores shared state,
such as the logged-in user.
\\
\\

\subsection{Core components}

The implementation is based on several core components.
\\
\\
\textbf{Identity:}
Each user owns an encrypted identity that is stored locally. The identity contains the user's display name, secret key and author identifier. After a successful login, the identity is decrypted and used to create the local networking node.
\\
\\
\textbf{Node:}
The node represents the local peer in the network. It creates and manages the Iroh endpoint, document engine, blob store, gossip service and router. It also keeps track of all spaces currently loaded by the user and acts as the connection between the frontend and the networking layer.
\\
\\
\textbf{Space:}
A space is comparable to a Discord server. It contains the shared information about a group, including its members and available rooms. This information is stored in replicated Iroh documents, allowing every participant to synchronize the same state.
\\
\\
\textbf{Room:}
Each space contains one or more rooms. Every room has its own replicated document in which chat messages are stored. Since each room has its own document, rooms can be synchronized independently.
\\
\\
\textbf{Invite:}
Users can join existing spaces through invite codes. An invite contains the information required to import the corresponding Iroh documents and connect to the space. Invites are stored locally so that spaces can be restored after restarting the application.
\\
\\
\textbf{Voice communication:}
Voice calls use a separate Iroh protocol. Audio is captured using CPAL, sent over Iroh datagrams and played back on the receiving peer. This allows voice communication without requiring a dedicated voice server.

\section{Synchronization workflow}

The synchronization starts when a user performs an action in the frontend, such as joining a space,
creating a room or sending a message. The request is forwarded to the Rust backend, where it is
handled by the local node.
\\
Blabber stores shared data in replicated Iroh documents. Each space is represented by a member
document, which stores the members of the space, and an information document, which stores the rooms. Every room has its own document containing the chat messages.
\\
To join a space, a user imports an invite. The invite contains the access
tickets required to import the space's information and member documents
through Iroh. After importing the information document, the backend reads
the available room records. Each record contains the ticket required to
import the corresponding room document, which stores the messages for that room.
\\
Whenever an imported document changes, Iroh synchronizes the update with the other peers that
have access to it. The node subscribes to the loaded room documents and listens for local and
remote message insertions. When a new message is detected, it is added to the local room cache
and sent as an application event. The frontend receives this event and displays the message in
the correct space and room.

\subsection{Persistence and application restart}

%% PROPOSAL (grammar, sentence fragment): "When a space is created or
%% joined." below is currently its own "sentence", ending in a period
%% with no verb for what happens -- an editing artifact. It reads
%% correctly if merged with the following sentence, matching what the
%% code actually does (Space::create_directory writes invite.txt when
%% a space is created or joined; Node::load_spaces reads all of them
%% back on startup). Suggested fix:
%%   "...Blabber persists the invite code used to join them: when a
%%   space is created or joined, its invite is written to disk. On
%%   startup we parse all the saved invites and then import the
%%   corresponding Iroh documents."
%% PROPOSAL (typo, still unfixed): "persistant" -> "persistent" (a few
%% lines below, "The only persistant artifact...").
To make spaces that a user has joined available again after the application is
closed and started again, Blabber persists the invite code used to join them.
When a space is created or joined. \\
On startup we parse all the saved invites and then import the corresponding Iroh documents.
Then \texttt{Space::sync\_rooms} is called to restore every room that space
contains. This means Blabber itself stores no metadata like membership information or chat data outside of the replicated Iroh documents. The only persistant artifact is the invite code
required to derive access to the documents.

\section{Group voice calls}
%% Verified against blabber-core/src/call_rooms.rs, node.rs
%% (join_mesh/join_call_room) and channel.rs (MeshVoiceChannel) --
%% content is accurate: full mesh, 16-byte room-id handshake + 1-byte
%% ack, one jitter buffer per peer, 10ms mix interval averaging all
%% non-empty buffers. Only language/grammar fixes proposed below.
%%
%% PROPOSAL (word choice): "a full end-to-end architecture" is
%% ambiguous -- "end-to-end" usually signals end-to-end *encryption* in
%% a security context, but here it is used to mean "fully peer-to-peer
%% (no relay/SFU)". Suggest: "a fully peer-to-peer architecture".
%% PROPOSAL (grammar): missing comma -- "Because we want to keep a
%% full end-to-end architecture, we did not include...".
%% PROPOSAL (typo): "though a dedicated protocol" -> "through a
%% dedicated protocol".
%% PROPOSAL (grammar): "receive an audio containing all the
%% participants voices" -- "audio" is uncountable (no "an audio"), and
%% "participants voices" is missing an apostrophe. Suggest: "receive a
%% single audio stream containing every participant's voice".
%% PROPOSAL (word choice): "the amount of participants" -> "the number
%% of participants" ("amount" is for uncountable quantities, "number"
%% for countable ones like people).
%% PROPOSAL (missing security discussion): unlike the 1:1 voice case,
%% which gets its own "Voice communication" subsection under Security
%% considerations, group calls have no corresponding security
%% discussion anywhere in the report. Worth at least one sentence here
%% or in Security considerations noting that group-call audio has the
%% same confidentiality story as 1:1 calls (Iroh's transport-level
%% QUIC/TLS encryption; no application-level key exchange is performed
%% for call-room connections either -- CallRoomProtocol::accept in
%% channel.rs never calls perform_key_exchange_as_initiator/acceptor).
Blabber not only supports voice communication between two participants but also between multiple through a mesh topology. Because we want to keep a full end-to-end architecture we did not include any forwarding of voice data or a relay unit. This means every participant needs to be connected to all others. The group call information that is needed for this connectivity is managed by a call room. It tracks known endpoint IDs and contains a call history through a replicated Iroh document. This is exactly the same pattern that is used for chat rooms. \\
Whenever a user joins the call room, the local node connects to all known users though a dedicated protocol, then sends a 16-byte call room identifier and waits for an acknowledgment before then registering the connection in a mesh voice channel. \\
Since all participants get one audio stream per participant, we need to mix them together on every client to receive an audio containing all the participants voices. We achieve this by having one jitter buffer per peer and mixing them together on a fixed time interval by averaging the buffered samples across all the participants that currently have data available. \\
With this design we avoid the need for a server but the number of needed connections scales quadratically with the amount of participants.

\section{Conflict-free replicated data structures}

Blabber uses the replicated document system provided by Iroh, which is based on conflict-free replicated data structures (CRDTs). A CRDT allows multiple peers to modify the same replicated data independently while ensuring that all replicas eventually converge to the same state without requiring a central server.
\\
In Blabber, spaces, members and chat messages are stored in replicated Iroh documents. Every peer maintains a local replica of the documents it has access to and can update them locally. These changes are synchronized automatically with the other peers through Iroh.
\\
Because Iroh handles replication and conflict resolution internally, the application does not need to implement its own synchronization or merge algorithms. This simplifies the implementation and it ensures that all peers eventually have the same state.

\section{Security considerations}
The most important goal of Blabber is privacy. There is no central server that stores user accounts, chat history or other shared data.
Blabber was designed with privacy as one of its main goals.  Every participant stores
only the information required for the spaces they have joined.

\subsection{Encrypted identities}

%% PROPOSAL (citation): first (and only) mention of Argon2 and
%% ChaCha20-Poly1305 in the report -- add references (e.g. RFC 9106 for
%% Argon2, RFC 8439 for ChaCha20-Poly1305).

Each user identity is encrypted before being stored on disk. During identity
creation, the user's password is used to derive an encryption key using
Argon2. This key encrypts the identity with ChaCha20-Poly1305 before it is
written to persistent storage. Therefore, the identity can only be restored with the correct password.
%% PROPOSAL (grammar/phrasing): "Our current software holds the
%% decrypt secret in plain in memory. An Attacker with memory access
%% could use a memory dump to retrieve said secret." -- "the decrypt
%% secret" is ambiguous (reads as "decryption secret", but this
%% actually means the identity's raw 32-byte secret key, [u8; 32] in
%% identity.rs, confirmed unchanged since it was flagged with a TODO
%% ("mlock this one and use Zeroing") in identity.rs); "in plain in
%% memory" -> "in plaintext in memory"; "An Attacker" should not be
%% capitalized; "said secret" is unnecessarily legalistic. Suggested:
%%   "Our current implementation holds the identity's secret key in
%%   plaintext in memory for the lifetime of the session. An attacker
%%   with memory access to a running instance could recover it via a
%%   memory dump."
%% Content-accuracy note: this is factually correct and matches the
%% code -- a "zeroize" dependency has since been added to Cargo.toml
%% (workspace) and blabber-core/Cargo.toml, but it is not actually
%% imported or used anywhere yet, so the limitation described here is
%% still fully open, not partially addressed.
Our current software holds the decrypt secret in plain in memory. An Attacker with memory access could use a memory dump to retrieve said secret. As a further improvement we could overwrite the secret with zeros on drop, lock the memory page and exclude it from core dumps.
\subsection{Authentication}

%% PROPOSAL (accuracy / requirement mismatch): Problem statement
%% security requirement (d) states "Prevent user impersonation by
%% authentication through password, nonce and username", but this
%% subsection describes a different mechanism (deterministic
%% public-key endpoint identity) and doesn't mention password, nonce
%% or username at all. Checked against the code:
%%   - The password only ever gates *local* decryption of the identity
%%     file at rest (identity.rs derive_key/store/load_from_disk) -- it
%%     is never sent to or checked by a peer, so it plays no role in
%%     peer-to-peer authentication.
%%   - "nonce" in the codebase refers only to the ChaCha20-Poly1305
%%     encryption nonce used for the identity file (identity.rs,
%%     NONCE_LEN), not to any network-level challenge/response nonce
%%     exchanged between peers.
%%   - "username" (display_name) is entirely self-asserted: when a peer
%%     joins/creates a space, Space::insert_self_as_member (space.rs)
%%     writes whatever display_name string it was given into the
%%     shared member document, with nothing binding it to the
%%     endpoint's cryptographic identity. Any peer could claim any
%%     other member's display name -- there is no username-impersonation
%%     protection today.
%% So the true mechanism is: peers are identified by their Iroh
%% endpoint's public key (self-certifying, since the same secret always
%% produces the same EndpointId), which prevents *endpoint*
%% impersonation, but does nothing to stop *display-name*
%% impersonation. Recommend rewriting this subsection to describe what
%% is actually implemented, and either move the display-name gap to
%% Limitations or note it explicitly here so the security requirement
%% claim isn't overstated.
Each identity contains a secret key that generates the same
Iroh endpoint whenever the user logs in. This allows peers to authenticate one
another through their cryptographic identities, so we don't need to rely on a central authentication server.

\subsection{Message synchronization}

Messages are stored inside replicated Iroh documents. Only users possessing the
required document tickets can import and synchronize the corresponding
documents. Because synchronization takes place directly between peers, no
intermediate server is needed to relay or permanently store chat data.
%% PROPOSAL (new content, verified against invite.rs/space.rs): worth
%% adding a sentence on the granularity of this access control. Ticket
%% possession is checked per-document, not per-key: Invite::from_space
%% (invite.rs) shares the member document with ShareMode::Write, and
%% Space::create_room / insert_self_as_member (space.rs) write their
%% own records under keys like "member/{author}" and "room/{uuid}",
%% but nothing in the doc itself stops any peer holding that same
%% write ticket from overwriting or deleting a *different* member's or
%% room's key -- the application only relies on well-behaved peers
%% writing their own keys. Suggested addition:
%%   "Access control operates at the granularity of a whole document:
%%   anyone holding a write ticket for a space's member or information
%%   document can write any key within it, not only their own entry.
%%   The current implementation relies on peers only writing their own
%%   records; a malicious member could overwrite another member's
%%   entry or a room record. Finer-grained, per-key permissions are not
%%   currently enforced."
%% This is a genuine, code-confirmed gap and a good Limitations-section
%% candidate if you'd rather keep this subsection shorter.

\subsection{Voice communication}
%% PROPOSAL (accuracy -- re-verified against current node.rs/channel.rs,
%% stronger than the earlier note): the first sentence below still
%% claims, as something that actually happens, that "the peers perform
%% a Diffie-Hellman key exchange" before transmitting audio. Checked
%% against the code: perform_key_exchange_as_initiator/acceptor and the
%% diffie_hellman() helper (node.rs) are defined and covered by a unit
%% test (test_dh_key_exchange_produces_matching_keys), but they are
%% NOT called from any real call-establishment path:
%%   - Node::call() (1:1 calls) connects and immediately wraps the
%%     connection in a VoiceChannel -- no key exchange.
%%   - VoiceProtocol::accept() (channel.rs, the inbound side of 1:1
%%     calls) also never calls it.
%%   - Node::join_mesh()/join_call_room() and CallRoomProtocol::accept()
%%     (group calls) don't call it either.
%% So today, no DH exchange happens during an actual call of either
%% kind -- it is unit-tested but otherwise dead code. Confidentiality
%% relies entirely on Iroh's transport-level QUIC/TLS encryption for
%% every voice path. Suggested rewrite:
%%   "Voice calls use a dedicated Iroh protocol that establishes a
%%   direct connection between two peers. The codebase includes a11
%%   Diffie-Hellman key-exchange routine intended to derive a shared
%%   secret for application-level encryption of the audio payload;
%%   however, it is currently only exercised by a unit test and is not
%%   yet invoked from either the one-to-one or group call
%%   establishment paths. Confidentiality of voice data today therefore
%%   relies entirely on the transport-level encryption already provided
%%   by Iroh's underlying QUIC connection; wiring the DH secret into
%%   the audio path for defense-in-depth is listed as future work."
%% Also note (typos in the current sentence if you keep it): "Irohs" ->
%% "Iroh's"; "voice data grams" -> "voice datagrams" (one word).
Voice calls use a dedicated Iroh protocol that establishes a direct connection
between two peers. Before transmitting audio, the peers perform a
Diffie-Hellman key exchange to derive a shared secret that could be used for
secure communication. Currently our implementation relies on Irohs QUIC / TLS encryption for the voice data grams but if we want to further increase the confidentiality of our calls we could use the DH secret for application-level encryption.

\section{Supported chat operations}

Blabber provides the core functionality expected from a modern chat application.
Users can perform the following operations:

\begin{itemize}
    \item Create an encrypted local identity.
    \item Log in using an existing identity.
    \item Create new spaces.
    \item Join existing spaces using an invite code.
    \item Create chat rooms within a space.
    \item Exchange text messages with all members of a room.
    \item Receive newly synchronized messages automatically.
    \item Start and end peer-to-peer voice calls.
\end{itemize}
%% PROPOSAL (accuracy, decide before submission): "Start and end
%% peer-to-peer voice calls" is true of blabber-core, but currently the
%% shipped frontend (blabber-app) does not expose any voice or
%% call-room commands -- the command file is present but entirely
%% commented out. Either (a) re-wire those commands before the report
%% is due so this bullet is literally true of the handed-in app, or
%% (b) rephrase, e.g. "Start and end peer-to-peer voice calls (backend
%% implemented; not yet wired into the current GUI)". Keep this
%% consistent with the same claim in "Achieved results" just below.

%% PROPOSAL (missing table): add a requirements-to-implementation
%% mapping table here, tying back to the (a)-(f) functional and (a)-(d)
%% security requirements from "Problem statement". Graders in a
%% requirements-driven course typically look for this explicitly.
%% Skeleton:
%%
%% \begin{tabular}{p{0.45\linewidth}p{0.45\linewidth}}
%% Requirement & Status / where implemented \\
%% \hline
%% (a) Group chats with specific rooms & Met -- Space + Room documents \\
%% (b) Shared chat history for all participants & Met -- replicated Iroh docs (CRDT) \\
%% (c) Message users not connected through rooms & ? -- check against
%%     current implementation, may not be met \\
%% (d) Receive missed messages on reconnect & Met -- sync\_rooms on
%%     startup / Iroh replication \\
%% (e) Start a call with another user & Backend: met. Frontend: not
%%     currently wired (see caveat above) \\
%% (f) Intuitive UI navigation & Subjective -- discuss briefly \\
%% Security (a) No stored user data except at endpoints & Met \\
%% Security (b) No information disclosure in transit & Met -- Iroh
%%     QUIC/TLS \\
%% Security (c) Encrypt critical data at rest & Met -- Argon2 +
%%     ChaCha20-Poly1305 identity encryption \\
%% Security (d) Prevent impersonation & Partially -- cryptographic
%%     endpoint identity, but check whether "nonce and username" from
%%     the requirement text is actually implemented \\
%% \end{tabular}

\section{Achieved results}
%Screenshots vom GUI
Our project achieved the primary objectives defined at the beginning of the semester.
\\
Users are able to create spaces, join existing spaces, communicate through multiple chat rooms and establish peer-to-peer voice calls. Shared chat data is synchronized between participants without relying on a central server and identities are encrypted on local devices.
\\
Overall, our application shows that a decentralized messaging application can provide functionality similar to conventional chat platforms while the user has control over their data.
%% PROPOSAL (accuracy): same caveat as above -- "establish peer-to-peer
%% voice calls" needs to match what the submitted app can actually do.
%% PROPOSAL (cleanup): "%Screenshots vom GUI" is a leftover German
%% scratch comment. Either add the screenshots here (this looks like
%% where they were meant to go) or delete the comment.

\section{Conclusion}
%% PROPOSAL: this section is currently empty (just a stray comment and
%% a \newpage). A report without a conclusion reads as unfinished
%% regardless of the quality of earlier sections. Suggested content
%% (adapt/shorten in your own words):
%%
%% Blabber demonstrates that a peer-to-peer chat and voice-call
%% application can be built without any central server, using Iroh's
%% replicated documents for shared state and QUIC-based direct
%% connections for both synchronization and audio transport. Of the
%% six functional requirements defined in Section~2, N were fully met,
%% M partially (name them); of the four security/privacy requirements,
%% N were fully met (name any gaps, e.g. the in-memory secret not being
%% zeroized, or the DH-derived secret not yet being used to encrypt
%% voice payloads). The main architectural limitation is the full-mesh
%% topology used for group calls, which does not scale past a small
%% number of participants; future work could explore a lightweight
%% relay/SFU approach, application-level encryption for voice using the
%% already-derived DH secret, and zeroizing secret key material in
%% memory. Overall, the project met its goal of giving its users a
%% self-hosted, privacy-respecting alternative to commercial chat
%% platforms for their own use.
%%
%% "%Mehr persönlich?" (leftover note-to-self, German for "more
%% personal?") -- resolve by writing the conclusion above, then delete
%% this comment.
\newpage

%% PROPOSAL (bibliography): none of the citation proposals above (Iroh,
%% Argon2, ChaCha20-Poly1305, CRDTs/Shapiro et al.) will render without
%% an actual bibliography. Simplest option for a report this size is a
%% manual thebibliography block right before \end{document}, e.g.:
%%
%% \begin{thebibliography}{9}
%% \bibitem{iroh} n0 Computer, Inc. \emph{Iroh}. https://iroh.computer
%% \bibitem{argon2} A. Biryukov, D. Dinu, D. Khovratovich. \emph{Argon2:
%%   the memory-hard function for password hashing}. RFC 9106, 2021.
%% \bibitem{chacha20poly1305} Y. Nir, A. Langley. \emph{ChaCha20 and
%%   Poly1305 for IETF Protocols}. RFC 8439, 2018.
%% \bibitem{crdt} M. Shapiro, N. Preguica, C. Baquero, M. Zawirski.
%%   \emph{Conflict-free Replicated Data Types}. SSS 2011.
%% \end{thebibliography}
%%
%% then cite with \cite{iroh}, \cite{argon2}, etc. at the spots marked
%% above.

\end{document}
